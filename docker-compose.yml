version: '3.9'

services:
  dev-db:
    image: postgres:13.1-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: W800serF
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
      POSTGRES_ADMIN_PASSWORD: W800serF